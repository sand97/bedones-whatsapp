name: Container Images

concurrency:
  group: container-images-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

on:
  pull_request:
    paths:
      - '.dockerignore'
      - '.github/workflows/container-images.yml'
      - 'apps/backend/**'
      - 'apps/whatsapp-agent/**'
      - 'apps/whatsapp-connector/**'
      - 'packages/common/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'scripts/upload-sentry-sourcemaps.sh'
  push:
    branches:
      - main
    paths:
      - '.dockerignore'
      - '.github/workflows/container-images.yml'
      - 'apps/backend/**'
      - 'apps/whatsapp-agent/**'
      - 'apps/whatsapp-connector/**'
      - 'packages/common/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'scripts/upload-sentry-sourcemaps.sh'
  workflow_dispatch:

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      whatsapp_agent: ${{ steps.filter.outputs.whatsapp_agent }}
      whatsapp_connector: ${{ steps.filter.outputs.whatsapp_connector }}
      shared: ${{ steps.filter.outputs.shared }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Detect changed paths
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            shared:
              - '.dockerignore'
              - '.github/workflows/container-images.yml'
              - 'packages/common/**'
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'pnpm-workspace.yaml'
              - 'scripts/upload-sentry-sourcemaps.sh'
            backend:
              - 'apps/backend/**'
            whatsapp_agent:
              - 'apps/whatsapp-agent/**'
              - 'apps/backend/swagger-output/**'
            whatsapp_connector:
              - 'apps/whatsapp-connector/**'

  build:
    needs: changes
    if: |
      github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.shared == 'true' ||
      (matrix.id == 'backend' && needs.changes.outputs.backend == 'true') ||
      (matrix.id == 'whatsapp-agent' && needs.changes.outputs.whatsapp_agent == 'true') ||
      (matrix.id == 'whatsapp-connector' && needs.changes.outputs.whatsapp_connector == 'true')
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: backend
            context: .
            dockerfile: apps/backend/Dockerfile
            image: bedones-whatsapp-backend
          - id: whatsapp-agent
            context: .
            dockerfile: apps/whatsapp-agent/Dockerfile
            image: bedones-whatsapp-agent
          - id: whatsapp-connector
            context: .
            dockerfile: apps/whatsapp-connector/Dockerfile
            image: bedones-whatsapp-connector

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compute registry namespace
        id: vars
        shell: bash
        run: |
          echo "owner=${GITHUB_REPOSITORY_OWNER,,}" >> "${GITHUB_OUTPUT}"
          echo "push=$([[ '${{ github.event_name }}' == 'push' && '${{ github.ref }}' == 'refs/heads/main' ]] && echo true || echo false)" >> "${GITHUB_OUTPUT}"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: steps.vars.outputs.push == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ steps.vars.outputs.owner }}/${{ matrix.image }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=main,enable={{is_default_branch}}

      - name: Build and optionally push image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: ${{ steps.vars.outputs.push == 'true' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.id }}
          cache-to: type=gha,mode=max,scope=${{ matrix.id }}
          platforms: linux/amd64
